---
title: API Reference
description: Zera Backend API Interface Documentation
---

This document is automatically generated from the OpenAPI specification and contains detailed descriptions of all backend API interfaces.

## Overview

The Zera backend provides API services using [Connect-RPC](https://connectrpc.com/). All interfaces support gRPC, gRPC-Web, and Connect protocols simultaneously.

### Available Services

- **AuthService** - User login, registration, logout, and token refresh
- **GreetService** - Example service for demonstrating basic usage

### Common Request Headers

All requests must include the following headers:

| Header | Description | Required |
|--------|-------------|----------|
| `Connect-Protocol-Version` | Connect protocol version, fixed to `1` | Yes |
| `Connect-Timeout-Ms` | Request timeout (in milliseconds) | No |
| `Authorization` | Authentication token (format: `Bearer <token>`) | Required for protected endpoints |

### Error Responses

All interfaces use the standard Connect error format:

```json
{
  "code": "not_found",
  "message": "Resource not found"
}
```

Error codes include: `canceled`, `unknown`, `invalid_argument`, `deadline_exceeded`, `not_found`, `already_exists`, `permission_denied`, `resource_exhausted`, `failed_precondition`, `aborted`, `out_of_range`, `unimplemented`, `internal`, `unavailable`, `data_loss`, `unauthenticated`